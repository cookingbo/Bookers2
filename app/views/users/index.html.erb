<h2>Users</h2>

<% @users.each do |user| %>
<div>
  <label>image</label>
  <%= link_to user_path(user.id) do %>
    <%= image_tag user.get_profile_image(100,100) %>
  <% end %>
  <label>name</label>
    <%= user.name %>
  <%= link_to "Show", user_path(user.id) %>
</div>
<% end %>

